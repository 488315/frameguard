# FrameGuard

FrameGuard is a local, human-in-the-loop visual change review demo built with React, TypeScript, Vite, and WebMCP.

## Local development

```sh
npm install
npm run dev
```

Run `npm test` for the test suite and `npm run build` for a production build.
